# Roadmap

<!-- Generated by harness. Do not hand-edit generated sections. -->

<!-- harness:begin generated -->
## Now

- No approved non-maintenance slice is recorded yet. Create one before broad implementation work.

## Next

- After the active slice lands, archive it from `docs/exec-plans/active/` into `docs/exec-plans/completed/`.
- Keep harness refresh work conditional instead of treating it as a default next task.

## Later

- Add richer repo-specific sequencing in Repo Notes once the product backlog is clearer.

## Do not repeat

- Do not choose harness-only work when `harness validate .` is green and `current-state.md` says maintenance is closed.
- Do not reopen completed work as if it were still the next approved slice.
- Do not start harness/status-doc maintenance unless the user asked for it, validation is red, or `current-state.md` says maintenance is open.
<!-- harness:end generated -->

## Repo Notes

- The next approved work here should start with a concrete exec plan rather than broad unsliced implementation.
- Keep maintenance closed unless docs or validation are actually broken.
